const optimizedPortManager = { async isPortAvailable(port) { return await new Promise((resolve) => { const img = new Image(); const timeout = setTimeout(() => { console.log(\); resolve(true); }, 2000); const handleResponse = (isLoad) => { clearTimeout(timeout); console.log(\); resolve(false); }; img.onload = () => handleResponse(true); img.onerror = () => handleResponse(false); img.src = \; }); } }; console.log('Testing port 3002 (should be available):'); optimizedPortManager.isPortAvailable(3002).then(result => console.log('Port 3002 available:', result));
